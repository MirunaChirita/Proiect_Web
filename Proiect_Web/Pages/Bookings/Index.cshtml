@page
@model Proiect_Web.Pages.Bookings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Member)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Rezervare.Partie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].DataProgramare)
            </th>
            <th>
                @Html.DisplayNameFor(model =>model.Booking[0].Rezervare.Monitor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Booking) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Member.FullName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Rezervare.Partie)
            </td>
               
            <td>
                @Html.DisplayFor(modelItem => item.DataProgramare)
            </td>
                 <td>
                    @Html.DisplayFor(modelItem => item.Rezervare.Monitor.NumeComplet)
                    </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
